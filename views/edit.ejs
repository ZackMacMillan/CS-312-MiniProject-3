<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Edit My Blog Post</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body class="bg-light">

  <div class="container py-5">
    <h2 class="text-primary-emphasis text-center">Revise Your Post</h2>

    <form method="POST" action="/posts/<%= post.blog_id %>" class="bg-white p-4 rounded shadow-sm">
      
      <div class="mb-3">
        <label for="title" class="form-label">Subject:</label>
        <input type="text" id="title" name="title" value="<%= post.title %>" class="form-control" required>
      </div>

      <div class="mb-3">
        <label for="body" class="form-label">Content:</label>
        <textarea id="body" name="content" rows="6" class="form-control" required><%= post.body %></textarea>
      </div>

      <div class="mb-3 text-muted">
        <small>Originally posted by <%= post.creator_name %> on <%= new Date(post.date_created).toLocaleString() %></small>
      </div>

      <div class="text-end">
        <button type="submit" class="btn btn-success">Save Changes</button>
        <a href="/" class="btn btn-secondary">Cancel</a>
      </div>
    </form>
  </div>

</body>
</html>